# Daily Memory — 2026-03-14

## Decisions
- For Aldi automation, Chromium on the VNC desktop is preferable to Epiphany; Epiphany is too unreliable for scripted input.
- The grocery project should prioritize **live pricing** as the next milestone; the burn-rate sheet is considered in a good enough state to support that work.
- Costco Same-Day / Instacart is the preferred next live-pricing target ahead of deeper Aldi cart work.
- Browser automation should not depend on the managed OpenClaw browser alone for hostile retail sites; the likely path is Jonathan’s real local browser session via relay/tunnel.

## Insights
- Chromium can run on the VNC desktop when launched with `--no-sandbox --disable-gpu` and `XAUTHORITY=/home/ubuntu/.Xauthority`; the prior problem was X auth, not Chromium itself.
- The OpenClaw browser/headless tool path was unavailable due to a gateway/browser timeout, so desktop-browser fallback was required.
- Aldi’s shopping UI is the current blocker, not account access: automation reached the site, but the cookie banner and store-selector modal caused inconsistent scripted clicks.
- Jonathan’s Raleigh-area Aldi target for future pricing/cart work is the store near `2900 Sunrise Valley Pl`.
- Once Jonathan manually selected Raleigh in the Aldi session, live Raleigh pricing could be retrieved from Aldi search results.
- Aldi redirects to login when attempting to add items to cart, so live price comparison can proceed without login but cart population requires an authenticated Aldi session.
- A live matched-staples comparison against the latest Walmart basket (2026-03-09) showed about `$10.62` savings on `$53.64` of overlapping Aldi-friendly items, roughly `19.8%` cheaper.
- Costco Same-Day in the managed OpenClaw browser appears to hit anti-bot / hostile-login friction, making it a poor primary path for authenticated shopping automation.
- Jonathan previously found that the browser relay extension only worked when tunneled to his local machine.
- `penguin` is the Chromebook Crostini Linux container, not the actual Chrome/Brave host; it cannot directly control the Chrome OS browser.
- The older Chromebook relay issue was not just networking: the local OpenClaw config on `penguin` previously lacked a `browser` section, so the relay/browser support was not starting there.

## Projects
- **Grocery Optimization Hub** — tested live Aldi cart-building automation on VNC as part of estimating real multi-store savings versus Walmart.
- **Grocery Optimization Hub** — completed a live Raleigh Aldi vs latest-Walmart matched-staples price check; cart automation remains blocked at Aldi login.
- **Grocery Optimization Hub** — pivoted next-step focus toward building a reliable live-pricing pipeline, with Costco Same-Day / Instacart as the first serious target.

## Areas
- Grocery optimization
- Browser automation
- VNC desktop control
- Live retail pricing
- Chromebook / relay troubleshooting

## Resources
- Aldi target location: `2900 Sunrise Valley Pl`, Raleigh, NC
- Working Chromium launch pattern on VNC: `/usr/bin/chromium-browser --no-sandbox --disable-gpu --user-data-dir=/tmp/chromium-aldi-test` with `DISPLAY=:1` and `XAUTHORITY=/home/ubuntu/.Xauthority`
- Chromebook/Linux-container user for tunnel work: `gustavbotty@penguin`
- Prior relay diagnosis references: `memory/2026-02-27-ssh-troubleshooting.md`, `memory/2026-02-26-relay-server.md`

## Tasks
- Resume Aldi pricing/cart build after cookie banner is accepted and Raleigh store is confirmed in Chromium.
- Compare live Aldi basket pricing against recent Walmart order history to estimate weekly savings.
- After Jonathan logs into Aldi, resume cart population using the previously matched Raleigh staples.
- Expand the comparison from the initial matched staples to a fuller weekly burn-rate demo basket.
- Verify the Chromebook / `penguin` relay setup and browser configuration before attempting local-browser control for Costco/Instacart.
- Build the live-pricing pipeline with Costco Same-Day / Instacart first, then broaden to other stores.

## Open Questions
- Does Jonathan want the browser interaction finished manually-first (he clears the blockers) or should pricing estimation proceed without live cart automation?
- Once logged in, should the next pass optimize for a strict 1-week Aldi demo basket or a broader household-stock-up basket?
- Can the Chromebook relay/browser-control path be made reliable enough through `penguin`, or will a different local host be needed for retail-site automation?

## Memory Flush
- Switched from Epiphany to Chromium for Aldi testing; Chromium works on VNC, but Aldi UI automation remains fragile.
- Raleigh Aldi pricing worked once the store was manually selected; add-to-cart immediately redirected to Aldi login, so the live comparison was completed as a price-match exercise instead of a full cart build.
- Jonathan wants the grocery project to become functional, with live pricing now treated as the most important next step.
- Costco Same-Day managed-browser testing ran into anti-bot / login friction.
- The likely long-term path is controlling Jonathan’s real local browser via relay/tunnel rather than relying on hostile-site automation in a managed browser.
- Jonathan is now leaning toward repurposing an old 2019 Intel MacBook Pro as the always-on OpenClaw/browser-automation host, with the VPS handling backend/data work and the Chromebook remaining the daily driver.
- MacBook setup progress: macOS Ventura `13.7.8`, Tailscale already present, Xcode Command Line Tools installed, Homebrew installed under `/usr/local`, Node installed via the official pkg after Homebrew source-build attempts proved too slow, and OpenClaw installed successfully (`2026.3.13`).
- On the MacBook, `openclaw status` showed the gateway was not yet running and LaunchAgents were not installed; the next immediate step is `openclaw gateway start`.

